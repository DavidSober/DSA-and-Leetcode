// first js code on leetcode its been what 7 years since i last used this lang?
// beats 93% in time and 27% in space

/**
 * @return {Function}
 */
var createHelloWorld = function() {
    return function(...args) {
        return 'Hello World'
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */